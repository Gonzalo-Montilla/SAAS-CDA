# Plantilla para build de producción (Vite carga `.env.production` al ejecutar `npm run build`).
# Este archivo SÍ puede versionarse. Los secretos no van aquí.
#
# Uso en tu PC:
#   copy env.production.template .env.production
# Edita `.env.production` con tu dominio real (ese archivo está en .gitignore).

VITE_API_URL=https://cdasoft.com.co/api/v1

# Turnstile: la configuración recomendada está en el BACKEND (.env: TURNSTILE_ENABLED, TURNSTILE_SECRET_KEY,
# TURNSTILE_SITE_KEY). El login lee GET /api/v1/config/turnstile-public — no hace falta site key en el build.
# Solo use las variables de abajo como respaldo si el API no responde (muy antiguo).
VITE_TURNSTILE_ENABLED=false
# VITE_TURNSTILE_SITE_KEY=
